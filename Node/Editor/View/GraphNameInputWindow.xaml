<Window x:Class="Node.Editor.View.GraphNameInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:Node.Localize"
        Title="{x:Static loc:TextUi.GraphNameInputTitle}"
        Width="320" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <StackPanel Margin="12">
        <TextBlock x:Name="PromptTextBlock" Margin="0,0,0,6" />
        <TextBox x:Name="NameTextBox" Margin="0,0,0,12" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static loc:TextUi.OkButtonLabel}"
                    Width="80" Margin="0,0,6,0"
                    IsDefault="True"
                    Click="OkButton_Click" />
            <Button Content="{x:Static loc:TextUi.CancelButtonLabel}"
                    Width="80"
                    IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>